config:
  target: "${api_url}"
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      x-api-key: "${api_key}"
scenarios:
  - flow:
    - post:
        url: "/logs"
        json:
          hello: "world"
